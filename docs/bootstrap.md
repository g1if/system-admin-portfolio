# Bootstrap System Installer

Автоматический установщик и настройщик системы для быстрого развертывания серверов с предустановленными инструментами администрирования.

## Назначение

Скрипт предназначен для автоматической настройки свежеустановленных Linux-серверов, включая установку необходимых пакетов, настройку безопасности и развертывание скриптов из портфолио.

## Возможности

- 🔄 **Автоматическое обновление** системы и пакетов
- 📦 **Установка базовых утилит** для администрирования
- 🛡️ **Настройка безопасности** (UFW, fail2ban, SSH)
- 📊 **Инструменты мониторинга** (htop, nmon, node-exporter)
- 🐳 **Установка Docker** и Docker Compose
- 🔧 **Развертывание скриптов** портфолио
- ⏰ **Настройка автоматических задач** через cron
- 👤 **Создание пользователей** с правами администратора

## Использование

### Базовая установка
```bash
# Скачать скрипт
git clone https://github.com/g1if/system-admin-portfolio.git
cd system-admin-portfolio

# Запуск полной установки (требует sudo)
sudo ./bootstrap.sh full

Варианты установки
bash

# Полная установка (рекомендуется)
sudo ./bootstrap.sh full

# Минимальная установка (только базовые пакеты)
sudo ./bootstrap.sh minimal

# Установка только Docker
sudo ./bootstrap.sh docker

# Справка
./bootstrap.sh help

Поддерживаемые системы
✅ Полная поддержка

    Ubuntu 18.04+, 20.04, 22.04, 24.04

    Debian 10+, 11, 12

⚠️ Частичная поддержка

    CentOS 7+, 8+

    RHEL 7+, 8+

Процесс установки
1. 🔍 Предварительные проверки

    Проверка прав root

    Определение дистрибутива и версии

    Проверка поддержки системы

2. 🔄 Обновление системы

    Обновление списка пакетов

    Установка обновлений безопасности

    Очистка кеша пакетов

3. 📦 Установка пакетов

Базовые утилиты:

    curl, wget, git, vim, htop

    net-tools, dnsutils

    python3, pip

    jq, yq, bc

Мониторинг:

    htop, iotop, iftop

    nmon, dstat, sysstat

    lm-sensors, smartmontools

    prometheus-node-exporter

Безопасность:

    fail2ban, unattended-upgrades

    ufw, auditd

    clamav, rkhunter

4. 🐳 Установка Docker

    Автоматическая установка Docker CE

    Установка Docker Compose

    Настройка прав пользователя

5. 🛡️ Настройка безопасности

    Настройка UFW (блокировка всех портов кроме SSH)

    Отключение root-логина по SSH

    Настройка парольной политики

    Настройка fail2ban

6. 🔧 Развертывание скриптов

    Настройка прав выполнения

    Создание симлинков в /usr/local/bin

    Генерация конфигурационных файлов

7. ⏰ Настройка автоматизации

    Еженедельное обновление системы

    Мониторинг каждые 5 минут

    Ежедневное резервное копирование

    Еженедельная проверка безопасности

После установки
Проверка системы
bash

# Проверить установленные скрипты
system-monitor.sh status
security-audit.sh quick

# Проверить работу Docker
docker --version
docker ps

# Проверить автоматические задачи
crontab -l

Рекомендуемые действия

    Настройте SSH ключи для безопасного доступа

    Проверьте настройки фаервола: ufw status

    Настройте оповещения: alert-system.sh config

    Настройте резервное копирование: backup-manager.sh config

    Перезагрузите систему для применения всех изменений

Логи и отчеты
Файлы логов

    Основной лог: logs/bootstrap.log

    Отчеты установки: logs/bootstrap-report-YYYYMMDD_HHMMSS.txt

Просмотр логов
bash

# Просмотр логов в реальном времени
tail -f logs/bootstrap.log

# Поиск ошибок
grep -i error logs/bootstrap.log

# Просмотр последнего отчета
ls -lt logs/bootstrap-report-* | head -1 | xargs cat

Кастомизация
Добавление пользовательских пакетов

Отредактируйте функции установки пакетов в скрипте:
bash

# В функции install_base_packages_ubuntu
apt-get install -y \
    ваши-дополнительные-пакеты \
    ...

Настройка cron задач

Измените функцию setup_cron_jobs() для добавления своих задач.
Создание пользователей

Используйте опцию создания пользователя во время установки или отредактируйте функцию create_admin_user().
Безопасность
🔒 Меры безопасности

    Автоматическое отключение root-логина по SSH

    Настройка UFW с политикой "deny by default"

    Установка и настройка fail2ban

    Настройка автоматических обновлений безопасности

⚠️ Важные замечания

    Скрипт отключает парольную аутентификацию SSH

    Обязательно настройте SSH ключи до запуска скрипта

    Проверьте настройки фаервола после установки

Пример использования
Для домашнего сервера
bash

# Полная установка с созданием пользователя
sudo ./bootstrap.sh full

Для продакшн-сервера
bash

# Минимальная установка + ручная настройка безопасности
sudo ./bootstrap.sh minimal

Для Docker-хоста
bash

# Установка только Docker и утилит
sudo ./bootstrap.sh docker

Устранение проблем
Распространенные проблемы

Проблема: Ошибка прав доступа
Решение: Запускайте скрипт с sudo

Проблема: Неподдерживаемый дистрибутив
Решение: Используйте поддерживаемую систему или добавьте поддержку в скрипт

Проблема: Ошибки сети при установке пакетов
Решение: Проверьте интернет-соединение и DNS
Диагностика
bash

# Проверить логи установки
cat logs/bootstrap.log

# Проверить системные логи
journalctl -u system-portfolio-monitor.service

# Проверить работу служб
systemctl status fail2ban
systemctl status docker

Разработка
Добавление поддержки нового дистрибутива

    Добавьте проверку в check_distro_support()

    Создайте функции установки: update_system_NEW(), install_base_packages_NEW()

    Протестируйте на целевой системе

Структура функций

    update_system_* - обновление системы

    install_base_packages_* - установка пакетов

    setup_* - настройка компонентов

Автор

g1if

Примечание: Всегда проверяйте скрипт перед запуском на продакшн-системах. Рекомендуется сначала протестировать в виртуальной среде.
